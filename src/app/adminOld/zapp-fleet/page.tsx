import React from "react";

export default function LiveDashboard() {
  return (
    <div>
      <h1 className="text-2xl font-bold mb-4">Live Dashboard</h1>
      <p>Real-time stats and overview go here (e.g., active users, fleet status).</p>
      {/* Add charts, metrics, or data fetched from APIs */}
    </div>
  );
}